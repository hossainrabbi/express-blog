<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/assets/css/styles.css">
    <title><%= title %> </title>
</head>
<body>
    <main class="container mx-auto px-20">
        <h1 class="text-3xl text-blue-700"><%= title %> </h1>
        <p class="text-gray-600">Welcome to <%= title %> </p>
        <br>
        <form action="/register" method="post">
            <input type="text" class="border block" name="name" required>
            <input type="text" class="border block my-3" name="username" required>
            <button type="submit" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5">Submit</button>
        </form>
        <br><br>

        <% if(user.length > 0) { %>
            <ol class="space-y-1 max-w-md list-decimal list-inside text-gray-500">
                <% for(i = 0; i < user.length; i++) { %> 
                    <li>Name: <%= user[i].name %> and Username: <%= user[i].username %> </li>
                <% } %> 
            </ol>
        <% } else { %>
            <div>Data Not Found</div>
        <% } %>
    </main>
</body>
</html>